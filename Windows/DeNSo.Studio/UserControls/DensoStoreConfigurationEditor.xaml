<UserControl x:Class="DeNSo.Studio.UserControls.DensoStoreConfigurationEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
  <Grid>
    <ScrollViewer>
      <StackPanel>
        <TextBlock Text="Store name" FontStyle="Italic" FontSize="11" Margin="10,10,0,0"/>
        <TextBox  Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" FontSize="14" Margin="5" />
      
        <TextBlock Text="Service Uri" FontStyle="Italic" FontSize="11" Margin="10,10,0,0"/>
        <TextBox Text="{Binding Localtion, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" FontSize="14" Margin="5" />

        <Button Content="Save" FontSize="12" Width="100" Height="30" Margin="0,10,5,0" HorizontalAlignment="Right" Click="Button_Click_1"/>
      </StackPanel>
    </ScrollViewer>
  </Grid>
</UserControl>
